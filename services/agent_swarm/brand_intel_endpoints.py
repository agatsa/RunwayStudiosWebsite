"""Brand Intel endpoints — appended to app.py via import or direct copy."""
# This file is used to generate the endpoint text that gets appended to app.py
# See: append_brand_intel_endpoints() call at bottom of app.py
